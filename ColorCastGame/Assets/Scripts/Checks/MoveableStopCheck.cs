using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MoveableStopCheck : MonoBehaviour
{
    /*private MovableStopObject movableStopObject;
    private Vector2 initialposition;

    public bool onObject = false;

    private void Start()
    {
        movableStopObject = GetComponent<MovableStopObject>(); 
        initialposition = new Vector2(transform.position.x, transform.position.y);
    }

    private void Update()
    {
        if (transform.position.x == initialposition.x && transform.position.y == initialposition.y)
        {
            movableStopObject.enabled = false;
            movableStopObject.enabled = true;
        }
    }

    private void OnCollisionStay2D(Collision2D collision)
    {
        if (collision.gameObject.tag == "Player")
        {
            onObject = true; 
        }
        else
        {
            onObject = false;
        }
    }

    */
}
